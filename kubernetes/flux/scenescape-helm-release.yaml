apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: scenescape
  namespace: scenescape
spec:
  releaseName: scenescape
  chart:
    spec:
      chart: ./kubernetes/scenescape-chart
      version: 1.0.0
      sourceRef:
        kind: GitRepository
        name: scenescape-git-repo
        namespace: flux-system
      interval: 10m
  interval: 10m
  values:
    repository: 10.190.167.131:5001